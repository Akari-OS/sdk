/* {{appName}} — 最小スタイル。
 * 配色・余白・ペイン枠は Shell のグローバルテーマ（Tailwind + semantic token）が提供するため、
 * ここでは root の高さ確保だけを行う（色をハードコードしない）。 */
:root {
  color-scheme: dark;
}
html,
body,
#root {
  height: 100%;
  margin: 0;
}
