# akari.toml — valid MCP-Declarative app fixture
# Used by certify tests as a known-good manifest.
# Spec: AKARI-HUB-024 §6.2 / §6.5

[app]
id       = "com.example.x-sender"
name     = "X Sender"
version  = "0.1.0"
author   = "Example Corp"
tier     = "mcp-declarative"
sdk      = ">=0.1.0 <1.0"
category = "publishing"

[mcp]
server = "mcp-servers/x-sender"
tools  = ["x.post", "x.schedule", "x.draft"]

[panels]
main = { title = "X", schema = "panels/x-sender.schema.json" }

[permissions]
"external-network" = ["api.x.com"]
oauth              = ["x.com"]

[agents]
x_sender_scheduler = "agents/scheduler.md"
