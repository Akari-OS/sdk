{
  "name": "@akari-os-examples/web-search",
  "version": "0.1.0",
  "description": "Web Search — AKARI MCP-Declarative App reference implementation (Research category)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "akari dev",
    "build": "tsup mcp-server/index.ts --format esm --target node18 --outDir dist/mcp-server --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@akari-os/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@tavily/core": "^0.0.2",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
