{
  "name": "@akari-os/sdk-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AKARI App SDK monorepo — CLI, types, renderer, and examples",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Akari-OS/sdk"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
